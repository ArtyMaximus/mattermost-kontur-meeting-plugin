# Kontur.Talk Meeting Plugin для Mattermost

Плагин добавляет кнопку для создания видеовстреч Kontur.Talk прямо из интерфейса Mattermost.

## Требования для сборки

- **Node.js** 14+ и npm
- **Go** 1.19+ (для серверного компонента)
- **tar** (встроен в Windows 10+)

## Сборка

### Автоматическая сборка (рекомендуется)

Используйте PowerShell скрипт для автоматической сборки и упаковки:

```powershell
.\build.ps1
```

Скрипт автоматически:
- Установит зависимости (npm)
- Соберет веб-часть через webpack
- Скомпилирует Go серверный компонент для Linux/Windows/macOS
- Создаст правильный **TAR.GZ** архив (требование Mattermost!)
- Проверит структуру архива

### Ручная сборка (Linux/macOS)

```bash
make dist
```

Или по шагам:

```powershell
# 1. Установка зависимостей веб-части
cd webapp
npm install
npm run build
cd ..

# 2. Компиляция серверной части
cd server
go mod download

# Linux
GOOS=linux GOARCH=amd64 go build -o dist/plugin-linux-amd64 plugin.go

# Windows  
GOOS=windows GOARCH=amd64 go build -o dist/plugin-windows-amd64.exe plugin.go

# macOS Intel
GOOS=darwin GOARCH=amd64 go build -o dist/plugin-darwin-amd64 plugin.go

# macOS Apple Silicon
GOOS=darwin GOARCH=arm64 go build -o dist/plugin-darwin-arm64 plugin.go

cd ..

# 3. Создание архива
tar -czf kontur-meeting.tar.gz plugin.json webapp/dist/ server/dist/
```

## Упаковка

⚠️ **ВАЖНО:** Mattermost требует формат `.tar.gz`, а не `.zip`!

### Windows PowerShell

```powershell
# Переходим в корень проекта
cd "E:\Program files\mattermost-kontur-meeting-plugin"

# Создаем временную структуру
New-Item -ItemType Directory -Path "build_temp\webapp\dist" -Force | Out-Null
Copy-Item "plugin.json" "build_temp\"
Copy-Item "webapp\dist\main.js" "build_temp\webapp\dist\"

# Создаем TAR.GZ архив (встроенный tar в Windows 10+)
cd build_temp
tar -czf "..\kontur-meeting.tar.gz" plugin.json webapp/dist/main.js
cd ..

# Проверяем содержимое
tar -tzf kontur-meeting.tar.gz

# Удаляем временную папку
Remove-Item -Path "build_temp" -Recurse -Force
```

### Linux/Mac

```bash
tar -czf kontur-meeting.tar.gz plugin.json webapp/dist/
```

## Установка в Mattermost

1. Откройте **System Console → Plugins → Plugin Management**
2. Нажмите **Upload Plugin**
3. Выберите файл `kontur-meeting.tar.gz` ⚠️ (формат .tar.gz обязателен!)
4. Нажмите **Upload**
5. Нажмите **Enable** для активации плагина

## Настройка

1. Откройте **System Console → Plugins → Kontur.Talk Meeting**
2. В поле **Webhook URL** введите URL вашего n8n webhook:
   ```
   http://host.docker.internal:5678/webhook/kontur-create
   ```
3. Нажмите **Save**
4. Перезагрузите страницу Mattermost (Ctrl+R или F5)

## Использование

1. Откройте любой канал или личное сообщение
2. В шапке канала появится кнопка с **иконкой видеокамеры** 📹 "Start Kontur Meeting"
3. Нажмите на кнопку
4. Плагин отправит запрос на webhook и создаст сообщение с ссылкой на встречу
5. Встреча автоматически откроется в новой вкладке браузера

**Важно:** Пользователям **не нужно ничего настраивать**. Webhook URL настраивается один раз администратором в System Console и работает для всех пользователей.

## Тестирование

### Требования к webhook

**📖 Подробная документация:** См. файл [WEBHOOK_API.md](WEBHOOK_API.md)

**Краткая справка:**

Webhook должен:
- Принимать POST запрос с JSON: `{channel_id, channel_name, channel_type, user_id, username}`
- Возвращать: `{room_url: "https://space.ktalk.ru/room/abc123"}`
- Status Code: `200 OK`
- Content-Type: `application/json`

### Mock webhook для тестирования (n8n)

Пока нет реального API Kontur.Talk, можно использовать тестовый webhook в n8n:

1. Создайте новый workflow в n8n
2. Добавьте узел **Webhook** (Trigger):
   - Method: POST
   - Path: `kontur-create`
   - Response Mode: `Respond to Webhook`
3. Добавьте узел **Function** (опционально для логики):
   ```javascript
   return {
     json: {
       room_url: `https://meet.google.com/test-${$json.channel_id}`,
       room_name: `Meeting - ${$json.channel_name}`,
       status: "success"
     }
   };
   ```
4. Добавьте узел **Respond to Webhook**:
   - Response Body: `{{ $json }}`
5. Активируйте workflow

### Проверка webhook через curl

**PowerShell:**
```powershell
$body = @{
    channel_id = "test123"
    channel_name = "Test Channel"
    channel_type = "O"
    user_id = "user123"
    username = "test.user"
} | ConvertTo-Json

Invoke-RestMethod -Uri "http://host.docker.internal:5678/webhook/kontur-create" `
    -Method POST -Body $body -ContentType "application/json"
```

**Ожидаемый ответ:**
```json
{
  "room_url": "https://meet.google.com/test-test123"
}
```

### Быстрый старт с n8n

1. **Импортируйте готовый workflow:**
   - Откройте n8n
   - Нажмите **Import from File**
   - Выберите файл `n8n-workflow-example.json`
   - Активируйте workflow

2. **Скопируйте URL webhook:**
   - Откройте узел "Webhook" в n8n
   - Скопируйте Production URL (например: `http://host.docker.internal:5678/webhook/kontur-create`)

3. **Настройте плагин:**
   - Вставьте URL в System Console → Plugins → Kontur.Talk Meeting → Webhook URL
   - Save

4. **Готово!** 🎉

## Для администраторов других компаний

### 🏢 Универсальность плагина

Этот плагин **универсальный** и работает в любой компании. При установке вам нужно:

1. **Установить плагин** в ваш Mattermost
2. **Настроить Webhook URL** в System Console
3. **Создать backend для создания встреч** (n8n или свой сервис)

### 🔧 Настройка Webhook URL

**Webhook URL** - это адрес **вашего сервиса**, который создаёт встречи Kontur.Talk.

#### Примеры:

**С n8n (рекомендуется):**
```
https://n8n.вашакомпания.com/webhook/kontur-create
```

**Свой backend:**
```
https://api.вашакомпания.com/meeting/create
```

**Локальный n8n для разработки:**
```
http://host.docker.internal:5678/webhook/kontur-create
```

### 📦 Что предоставить backend разработчикам:

1. **API документация:** [WEBHOOK_API.md](WEBHOOK_API.md)
2. **Готовый n8n workflow:** [n8n-workflow-example.json](n8n-workflow-example.json)
3. **Требования:** Backend должен принимать POST запрос и возвращать `{"room_url": "https://..."}`

### 🌐 Архитектура

```
Ваш Mattermost
    ↓ (POST запрос с данными канала/пользователя)
Ваш n8n или backend
    ↓ (вызов Kontur.Talk API)
Kontur.Talk API (api.ktalk.ru)
    ↓ (возврат room_url)
Сообщение в канале Mattermost
```

---

## Структура проекта

```
kontur-meeting-plugin/
├── plugin.json                    # Манифест плагина
├── server/
│   ├── plugin.go                 # Серверная часть (Go)
│   ├── go.mod                    # Go зависимости
│   ├── go.sum                    # Go зависимости lock
│   └── dist/                     # Скомпилированные бинарники (создаются после build.ps1)
│       ├── plugin-linux-amd64
│       ├── plugin-darwin-amd64
│       ├── plugin-darwin-arm64
│       └── plugin-windows-amd64.exe
├── webapp/
│   ├── src/
│   │   └── index.js              # Основной код плагина (клиентская часть)
│   ├── dist/
│   │   └── main.js               # Собранный bundle (создается после npm run build)
│   ├── package.json              # npm конфигурация
│   └── webpack.config.js         # webpack конфигурация
├── build.ps1                      # Скрипт автоматической сборки
├── Makefile                       # Сборка для Linux/macOS
├── README.md                      # Документация
├── WEBHOOK_API.md                 # Детальная документация по Webhook API
├── QUICKSTART.md                  # Быстрый старт
└── n8n-workflow-example.json     # Готовый n8n workflow для импорта
```

## Требования

- Mattermost Server 7.8.0 или выше (совместимо с Time Messenger в Skyeng)
- Node.js 14.0.0 или выше (для сборки)
- n8n или другой webhook endpoint для создания встреч

## Отладка

Плагин выводит подробные логи в консоль браузера (F12 → Console):

- Инициализация плагина
- Загрузка конфигурации
- Данные текущего пользователя
- Запросы к webhook
- Создание постов
- Ошибки

## Чеклист проверки

### Сборка:
- [ ] `cd webapp && npm install` выполнен успешно
- [ ] `npm run build` создал файл `webapp/dist/main.js`
- [ ] **TAR.GZ** архив содержит `plugin.json` и `webapp/dist/main.js` (не .zip!)
- [ ] Размер архива ~2-3 KB

### Требования Mattermost:
- [ ] ✅ plugin.json в корне архива
- [ ] ✅ ID формата: `^[a-zA-Z0-9-_\.]+$` (3-190 символов)
- [ ] ✅ Архив в формате `.tar.gz` (не .zip)
- [ ] ✅ `webapp.bundle_path` = `webapp/dist/main.js`
- [ ] ✅ `server: {}` присутствует
- [ ] ✅ `min_server_version` указана

### Установка и тестирование:
- [ ] Плагин загружен в Mattermost без ошибок
- [ ] Плагин включен (Enable)
- [ ] Webhook URL настроен в System Console
- [ ] Страница перезагружена (Ctrl+R)
- [ ] Кнопка 🎥 видна в шапке канала
- [ ] При клике n8n получает запрос
- [ ] Сообщение появляется в канале от имени текущего пользователя
- [ ] Встреча открывается в новой вкладке (если настройка включена)
- [ ] Работает в публичных каналах
- [ ] Работает в приватных каналах
- [ ] Работает в личных сообщениях

## Возможные ошибки

### "permission denied" при запуске плагина

**Проблема:** У исполняемых файлов нет прав на выполнение.

**Решение:**
- Пересоберите плагин с помощью `.\build.ps1` (требуется WSL или Git Bash)
- Или исправьте существующий архив:
  ```bash
  # Распаковать
  tar -xzf kontur-meeting.tar.gz
  
  # Установить права
  chmod +x server/dist/plugin-*
  
  # Упаковать заново
  tar -czf kontur-meeting.tar.gz plugin.json webapp/dist/ server/dist/
  ```

### "Webhook URL is not configured"
- Убедитесь, что Webhook URL настроен в System Console
- Перезагрузите страницу после настройки

### "Failed to create meeting. Check that the n8n webhook is running"
- Проверьте, что n8n запущен
- Проверьте, что URL webhook доступен из браузера
- Для Docker: используйте `host.docker.internal` вместо `localhost`

### "Failed to post message to channel"
- Убедитесь, что пользователь имеет права на отправку сообщений в канал
- Проверьте версию Mattermost (требуется 7.8.0+)

## Лицензия

MIT

