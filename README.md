# Плагин Kontur.Talk Meeting для Mattermost

Плагин для Mattermost, позволяющий пользователям создавать видеовстречи Kontur.Talk прямо из каналов Mattermost. Плагин поддерживает как мгновенные встречи, так и запланированные встречи с удобным интерфейсом.

## Описание

Плагин интегрирует Mattermost с Kontur.Talk (или любым совместимым видеосервисом) через архитектуру на основе webhook. Пользователи могут создавать встречи одним кликом из заголовка любого канала, а администраторы настраивают webhook endpoint один раз в System Console.

**Основные возможности:**
- **Мгновенные встречи**: Создать и присоединиться к встрече немедленно
- **Запланированные встречи**: Планировать встречи с указанием даты, времени, длительности и участников
- **Интеграция с каналами**: Работает в публичных каналах, приватных каналах и личных сообщениях
- **Автоматическая публикация**: Создаёт пост в канале со ссылкой на встречу
- **Удобный интерфейс**: React-модалка для планирования с пресетами времени и поиском участников

## Архитектура

### Backend (Go)

Серверный компонент — это плагин Mattermost на Go, который обрабатывает:

- **Маршрутизация HTTP-запросов**: Направляет запросы к соответствующим обработчикам (`/config`, `/api/schedule-meeting`)
- **Валидация запросов**: Проверяет входящие запросы (даты, длительность, участники)
- **Интеграция с Mattermost API**: Получает информацию о пользователях и каналах, создаёт посты
- **Общение с webhook**: Отправляет запросы на внешний webhook (n8n) и обрабатывает ответы
- **Обработка ошибок**: Структурированные ответы об ошибках с защитой от паник

**Ключевые файлы:**
- `server/plugin.go` - Инициализация плагина и HTTP-маршрутизация
- `server/schedule_handler.go` - Бизнес-логика планирования встреч
- `server/helpers.go` - Утилиты для безопасных вызовов API
- `server/constants.go` - Константы и значения конфигурации

### Frontend (React)

Компонент webapp предоставляет пользовательский интерфейс:

- **Кнопка в заголовке канала**: Выпадающее меню с опциями мгновенных и запланированных встреч
- **Модалка планирования встречи**: Полнофункциональная форма для планирования встреч
- **Поиск участников**: Поиск пользователей в реальном времени с debouncing
- **Пресеты времени**: Быстрый выбор (15 мин, 30 мин, 1 час, 2 часа)
- **Валидация формы**: Клиентская валидация с маппингом ошибок сервера

**Ключевые файлы:**
- `webapp/src/index.js` - Инициализация плагина и рендеринг React-компонентов
- `webapp/src/components/schedule_meeting_modal.jsx` - Основной компонент модалки планирования
- `webapp/src/components/modal_components.jsx` - Переиспользуемые компоненты формы
- `webapp/src/utils/` - Хелперы, константы и утилиты логирования

### Внешняя интеграция

Плагин общается с внешним webhook-сервисом (обычно n8n), который:

1. Получает запросы на создание встреч от плагина
2. Создаёт комнаты в Kontur.Talk (или другом видеосервисе)
3. Возвращает URL комнат плагину

Подробные требования к API см. в [WEBHOOK_API.md](WEBHOOK_API.md).

## Структура проекта

```
mattermost-kontur-meeting-plugin/
├── plugin.json                    # Манифест плагина (ID, версия, исполняемые файлы)
├── assets/
│   └── icon.svg                   # Иконка плагина
├── server/                        # Backend (Go)
│   ├── plugin.go                  # Точка входа плагина, HTTP-маршрутизация
│   ├── schedule_handler.go        # Бизнес-логика планирования встреч
│   ├── helpers.go                 # Безопасные обёртки API, обработка ошибок
│   ├── constants.go               # Константы (таймауты, имена полей)
│   ├── go.mod                     # Зависимости Go
│   └── dist/                      # Скомпилированные бинарники (генерируются)
│       ├── plugin-linux-amd64
│       ├── plugin-darwin-amd64
│       ├── plugin-darwin-arm64
│       └── plugin-windows-amd64.exe
├── webapp/                        # Frontend (React)
│   ├── src/
│   │   ├── index.js               # Инициализация плагина, рендеринг React
│   │   ├── components/
│   │   │   ├── schedule_meeting_modal.jsx    # Основная модалка планирования
│   │   │   ├── modal_components.jsx         # Переиспользуемые компоненты формы
│   │   │   └── kontur_icon.jsx              # SVG-компонент иконки
│   │   └── utils/
│   │       ├── helpers.js         # Форматирование ошибок, информация о пользователе
│   │       ├── constants.js       # Имена полей, таймзона
│   │       └── logger.js          # Утилита отладочного логирования
│   ├── webpack.config.js          # Конфигурация webpack
│   ├── package.json               # npm зависимости
│   └── dist/                      # Собранный bundle (генерируется)
│       └── main.js
├── build.ps1                      # Скрипт сборки PowerShell (Windows)
├── Makefile                       # Скрипт сборки Make (Linux/macOS)
├── build-archive.sh               # Помощник создания архива
├── README.md                       # Этот файл
├── WEBHOOK_API.md                  # Документация Webhook API
├── QUICKSTART.md                   # Руководство по быстрому старту
└── n8n-workflow-example.json      # Пример workflow n8n

```

## Требования

### Требования для сборки

- **Go** 1.19 или выше
- **Node.js** 14.0.0 или выше
- **npm** (входит в Node.js)
- **tar** (встроен в Windows 10+, стандарт для Linux/macOS)

### Требования для запуска

- **Mattermost Server** 7.8.0 или выше
- **Webhook endpoint** (n8n или собственный backend) настроен и доступен

## Установка и настройка

### Сборка плагина

#### Windows (PowerShell)

```powershell
.\build.ps1
```

Скрипт выполнит:
1. Установку npm зависимостей
2. Сборку webapp bundle через webpack
3. Компиляцию Go серверных бинарников для всех платформ
4. Создание архива `kontur-meeting.tar.gz`

#### Linux/macOS

```bash
make dist
```

Или вручную:

```bash
# Сборка webapp
cd webapp
npm install
npm run build
cd ..

# Сборка серверных бинарников
cd server
go mod download
GOOS=linux GOARCH=amd64 go build -o dist/plugin-linux-amd64 .
GOOS=darwin GOARCH=amd64 go build -o dist/plugin-darwin-amd64 .
GOOS=darwin GOARCH=arm64 go build -o dist/plugin-darwin-arm64 .
GOOS=windows GOARCH=amd64 go build -o dist/plugin-windows-amd64.exe .
cd ..

# Создание архива
tar -czf kontur-meeting.tar.gz plugin.json webapp/dist/ server/dist/ assets/
```

### Установка в Mattermost

1. Откройте **System Console → Plugins → Plugin Management**
2. Нажмите **Upload Plugin**
3. Выберите `kontur-meeting.tar.gz` (⚠️ должен быть `.tar.gz`, не `.zip`)
4. Нажмите **Upload**
5. Нажмите **Enable** для активации плагина

### Конфигурация

1. Откройте **System Console → Plugins → Kontur.Talk Meeting**
2. В разделе настроек плагина найдите следующие поля:

   **Webhook URL** (обязательно)
   - Введите URL вашего n8n webhook или собственного backend endpoint
   - Пример: `https://n8n.example.com/webhook/kontur-create`
   - Для локальной разработки: `http://host.docker.internal:5678/webhook/kontur-create`

   **Open In New Tab** (опционально, по умолчанию: включено)
   - Если включено, ссылки на встречи автоматически открываются в новой вкладке браузера

   **Название сервиса видеосвязи** (опционально, по умолчанию: "Kontur.Talk")
   - Поле называется **"Название сервиса видеосвязи"** в интерфейсе настроек
   - Введите название вашего сервиса видеосвязи (например: "Zoom", "Google Meet", "Microsoft Teams")
   - Это название будет отображаться в интерфейсе плагина (кнопки, заголовки модалок)
   - Если оставить пустым, используется общий термин "видеосвязи"

3. Нажмите **Save**
4. Перезагрузите страницу Mattermost (Ctrl+R или F5)

## Использование

### Для конечных пользователей

#### Мгновенная встреча

1. Откройте любой канал (публичный, приватный или личное сообщение)
2. Нажмите на иконку видеокамеры 📹 в заголовке канала
3. Выберите **"Созвониться сейчас"**
4. Встреча создаётся и в канале появляется пост со ссылкой на встречу
5. Встреча открывается в новой вкладке (если настроено)

#### Запланированная встреча

1. Нажмите на иконку видеокамеры 📹 в заголовке канала
2. Выберите **"Запланировать встречу"**
3. Заполните форму:
   - **Дата и время**: Выберите дату и время (или используйте пресеты: 15 мин, 30 мин, 1 час, 2 часа)
   - **Продолжительность**: Выберите от 15 минут до 4 часов
   - **Название**: Опциональное название встречи (максимум 100 символов)
   - **Участники**: Найдите и добавьте участников (автоматически добавляются для личных сообщений)
4. Нажмите **"Создать встречу"**
5. В канале появляется пост с деталями встречи

**Примечание**: Настройка пользователем не требуется. Webhook URL настраивается один раз администратором.

## Разработка

### Настройка локальной разработки

1. **Клонируйте репозиторий**
   ```bash
   git clone <repository-url>
   cd mattermost-kontur-meeting-plugin
   ```

2. **Настройте webapp**
   ```bash
   cd webapp
   npm install
   ```

3. **Настройте server**
   ```bash
   cd server
   go mod download
   ```

4. **Сборка для разработки**
   ```bash
   # Webapp (режим watch)
   cd webapp
   npm run build  # Пересобрать после изменений
   
   # Server (пересобрать после изменений)
   cd server
   go build -o dist/plugin-linux-amd64 .
   ```

5. **Тестирование локально**
   - Загрузите плагин в локальный экземпляр Mattermost
   - Настройте webhook URL, указывающий на ваш экземпляр n8n
   - Протестируйте мгновенные и запланированные встречи

### Стиль кода и соглашения

#### Backend (Go)

- **Обработка ошибок**: Используйте структурированные ошибки с контекстом. Избегайте логирования PII (emails, имена).
- **Логирование**: Используйте `LogDebug` для логов разработки, `LogInfo` только для критических событий, `LogError` для ошибок.
- **Константы**: Выносите магические числа и строки в `constants.go`.
- **Валидация**: Валидируйте запросы рано, возвращайте структурированные ответы об ошибках.
- **Организация файлов**: Храните бизнес-логику в `schedule_handler.go`, утилиты в `helpers.go`.

#### Frontend (JavaScript/React)

- **Компоненты**: Держите компоненты сфокусированными и переиспользуемыми. Выносите сложную логику в хуки или утилиты.
- **Управление состоянием**: Используйте React хуки (`useState`, `useEffect`) для состояния компонентов.
- **Обработка ошибок**: Используйте утилиту `formatErrorMessage` для единообразных сообщений об ошибках.
- **Логирование**: Используйте утилиту `logger` с режимом отладки (`window.KONTUR_DEBUG = true`).
- **Константы**: Используйте константы из `utils/constants.js` вместо магических строк.

### Отладка

#### Консоль браузера

Включите отладочное логирование:
```javascript
window.KONTUR_DEBUG = true;
```

Затем перезагрузите страницу. Все логи плагина появятся в консоли с префиксом `[Kontur]`.

#### Логи сервера

Проверьте логи сервера Mattermost на ошибки backend:
- Инициализация плагина
- Обработка HTTP-запросов
- Ошибки общения с webhook
- Ошибки создания постов

## Известные ограничения и TODO

### Текущие ограничения

1. **Таймзона**: В настоящее время жёстко задана как `Europe/Moscow` (+03:00). Планируется поддержка множественных таймзон.
2. **Retry для webhook**: Нет автоматического механизма повторных попыток для неудачных запросов к webhook.
3. **Создание поста**: Если создание поста не удалось после успешного webhook, пользователь может не увидеть ссылку на встречу.

### Технический долг

1. **Большие файлы**: `index.js` (504 строки) и `schedule_meeting_modal.jsx` (766 строк) должны быть рефакторены в меньшие модули.
2. **Глобальные переменные**: Используется `window.KonturMeetingPlugin` для общения между компонентами. Рассмотреть React Context.
3. **Дублирование валидации**: Логика валидации даты/времени дублируется между frontend и backend.

### Планируемые улучшения

- [ ] Добавить механизм retry для запросов к webhook
- [ ] Поддержка множественных таймзон (пользовательские или настраиваемые)
- [ ] Рефакторинг больших компонентов в меньшие модули
- [ ] Добавить unit-тесты для backend и frontend
- [ ] Добавить метрики/мониторинг для создания встреч
- [ ] Миграция на TypeScript для лучшей типобезопасности

## Решение проблем

### Плагин не загружается

**Симптомы**: Кнопка не появляется в заголовке канала

**Решения**:
1. Убедитесь, что плагин включён в System Console
2. Перезагрузите страницу Mattermost (Ctrl+R)
3. Проверьте консоль браузера (F12) на ошибки JavaScript
4. Убедитесь, что `webapp/dist/main.js` существует и включён в архив

### "Webhook URL is not configured"

**Решения**:
1. Перейдите в System Console → Plugins → Kontur.Talk Meeting
2. Введите webhook URL
3. Нажмите Save
4. Перезагрузите страницу

### "Failed to create meeting"

**Симптомы**: Ошибка alert при создании встречи

**Решения**:
1. Убедитесь, что webhook URL доступен (проверьте через curl)
2. Проверьте, что workflow n8n активирован
3. Убедитесь, что webhook возвращает `{"room_url": "..."}` в ответе
4. Проверьте логи сервера на детальные сообщения об ошибках

### Ошибки Permission Denied

**Симптомы**: `permission denied` при запуске плагина

**Решения**:
1. Пересоберите плагин с помощью `.\build.ps1` (использует WSL для правильных прав)
2. Или вручную исправьте права на сервере Mattermost:
   ```bash
   chmod +x /opt/mattermost/plugins/com.skyeng.kontur-meeting/server/dist/plugin-*
   ```

## Webhook API

Плагин общается с внешним webhook-сервисом. См. [WEBHOOK_API.md](WEBHOOK_API.md) для:

- Формат запроса/ответа
- Обработка ошибок
- Примеры реализации
- Руководство по тестированию

## Вклад в проект

1. Следуйте руководству по стилю кода выше
2. Тестируйте изменения локально перед отправкой
3. Обновляйте документацию при добавлении новых функций
4. Делайте коммиты сфокусированными и описательными

## Лицензия

MIT

## Поддержка

- **Issues**: https://github.com/skyeng/mattermost-kontur-meeting-plugin/issues
- **Документация плагинов Mattermost**: https://developers.mattermost.com/integrate/plugins/
- **Руководство по быстрому старту**: См. [QUICKSTART.md](QUICKSTART.md)
